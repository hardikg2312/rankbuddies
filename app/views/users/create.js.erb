<% if @user.save %>
	<% session[:user_id] = @user.id %>
	$(window.location.replace("<%= users_path %>"))  
<% else %>
	$('.sign_up_errors').empty().append("<%= j @user.errors.full_messages.first %>")
<% end %>